<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.9.xsd">

    <changeSet id="05-create-refresh_tokens-table" author="Тимофей Зайцев">
        <preConditions onFail="HALT">
            <not>
                <tableExists tableName="contracts"/>
            </not>
        </preConditions>
        <createTable tableName="refresh_tokens">
            <!-- ID токена (UUID) -->
            <column name="id" type="UUID">
                <constraints primaryKey="true"
                             primaryKeyName="pk_refresh_tokens"
                             nullable="false"/>
            </column>

            <!-- Сам refresh token (уникальный) -->
            <column name="token" type="VARCHAR(500)">
                <constraints nullable="false"
                             unique="true"
                             uniqueConstraintName="uk_refresh_token"/>
            </column>

            <column name="user_id" type="UUID">
                <constraints nullable="false"/>
            </column>

            <!-- Дата истечения токена -->
            <column name="expiry_date" type="TIMESTAMP WITH TIME ZONE">
                <constraints nullable="false"/>
            </column>

            <!-- Флаг отозван ли токен (можно добавить позже) -->
            <!-- <column name="revoked" type="BOOLEAN" defaultValue="false"/> -->
        </createTable>
    </changeSet>
</databaseChangeLog>